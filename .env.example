# Optional local overrides and secrets — copy to .env (gitignored).
#
# Shared project config (ports, paths, names) lives in config.env (committed).
# Only use .env for things you do NOT want in git, e.g.:
#
# SECRET_KEY=change-me
# DATABASE_URL=postgres://...
