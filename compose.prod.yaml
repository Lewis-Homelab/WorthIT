# Homelab production overrides — used by scripts/deploy.sh

services:
  api:
    restart: always

  web:
    restart: always

  db:
    restart: always
    volumes:
      - /home/lewis/homelab/data/WorthIT/postgres:/var/lib/postgresql/data
